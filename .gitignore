# Кэш скрапера — накопительная база, её тоже коммитим (нужна для Actions),
# но при желании можно раскомментировать, чтобы не хранить в git:
# .cache/

# Локальные файлы агентов (не публикуем)
AGENTS.md
CLAUDE.md
.cursorrules

# Системное
node_modules/
*.log
.DS_Store
